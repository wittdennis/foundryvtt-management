# yaml-language-server: $schema=https://raw.githubusercontent.com/argoproj/argo-workflows/main/api/jsonschema/schema.json
---
apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: scale-workload
spec:
  entrypoint: scale-workload
  artifactGC:
    strategy: OnWorkflowDeletion
    serviceAccountName: foundry-workflows
    podMetadata:
      labels:
        network-policy/allow-workflows-s3-egress: apply
        network-policy/allow-kube-apiserver-traffic: apply
  podGC:
    strategy: OnWorkflowSuccess
  podMetadata:
    labels:
      network-policy/allow-kube-apiserver-traffic: apply
      network-policy/allow-workflows-s3-egress: apply
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  arguments:
    parameters:
      - name: replicaCount
      - name: name
        default: foundry
      - name: type
        default: statefulset
      - name: timeout
        default: 120s
  templates:
    - name: scale-workload
      serviceAccountName: foundry-workflows
      automountServiceAccountToken: true
      inputs:
        parameters:
          - name: replicaCount
          - name: name
          - name: type
          - name: timeout
            default: 120s
      container:
        image: git.derwitt.site/containers/devops-tools:1.3.1 # renovate
        securityContext:
          capabilities:
            drop:
              - ALL
          runAsGroup: 1000
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          seccompProfile:
            type: RuntimeDefault
        command:
          - sh
          - -c
        args:
          - |
            set -eu

            kubectl scale --replicas={{inputs.parameters.replicaCount}} -n foundry {{inputs.parameters.type}}/{{inputs.parameters.name}}

            if [ "{{inputs.parameters.replicaCount}}" = "0" ]; then
              kubectl wait --for=delete pod -n foundry -l app.kubernetes.io/instance={{inputs.parameters.name}} --timeout={{inputs.parameters.timeout}}
            else
              kubectl rollout status -n foundry {{inputs.parameters.type}}/{{inputs.parameters.name}} --timeout={{inputs.parameters.timeout}}
            fi
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
